# Cleanup Record — 2026-07-04

本次只删除可再生或已明确失效的文件，不删除正式实验账本、原始数据、checkpoint、论文
证据、第三方源码或 git metadata。

已删除：

- 项目与 third-party 内的 `__pycache__`、`.pytest_cache` 和 `.pyc`；
- 旧 `synthetic_mcgs_*smoke*` 与 `synthetic_colmap`：RGB 和几何没有共同成像 GT，文档
  已明确禁止将其作为论文证据；
- `analytic_sphere_projected_*smoke*`：仅用于早期 reload/interface 检查，现有测试与
  正式 pipeline 已覆盖；
- `plane_s0_*_interface_10`：10-step 接口 smoke，正式 identifiability ladder 已替代。

有意保留：

- `experiments/benchmarks/` 和 `experiments/external/`：包含 claim/negative-result 证据；
- third-party 仓库及其 `.git`：用于固定 commit、复现实验和审计修改；
- SuGaR output 与 external pilot 的表面重复文件：前者是原生 baseline checkpoint，后者
  是冻结评测输入，暂不在没有内容哈希清单的情况下去重；
- `新建 文本文档.txt`：文件名不规范，但内容是仍有用的论文标题候选，未擅自删除。

## 追加 — 2026-07-07

- 重新清理项目与 third-party 的 `__pycache__` / `*.pyc` / `.pytest_cache`（可再生）；
- 经用户确认后删除 `sugar-conda-linux-x86_64.tar.gz`（3.7GB，sugar conda 环境的离线
  还原包）。删除前已核实 `sugar` 环境仍完整存在于 `/root/autodl-tmp/conda/envs/sugar`，
  且 `environment-sugar.yml` 保留可重建；仓库体积由 5.1GB 降至 1.5GB。
- 仍保留 `新建 文本文档.txt`（同上，论文标题候选）。

